{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Review Fix Loop Gate Config",
  "type": "object",
  "required": ["version", "modes", "slices", "gates"],
  "properties": {
    "version": { "const": 1 },
    "rule_files": {
      "type": "array",
      "items": { "type": "string" }
    },
    "modes": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["scope"],
        "properties": {
          "baseline": { "type": "string" },
          "scope": {
            "type": "array",
            "items": {
              "enum": ["staged", "unstaged", "untracked", "merge_base_to_head", "all"]
            }
          },
          "require_fresh_snapshot": { "type": "boolean" },
          "require_risk_slices": { "type": "boolean" },
          "require_invariant_checks": { "type": "boolean" },
          "require_residual_risk_report": { "type": "boolean" },
          "requires_merge_base": { "type": "boolean" },
          "requires_final_pass": { "type": "boolean" },
          "requires_repo_map": { "type": "boolean" },
          "requires_residual_risk_report": { "type": "boolean" },
          "max_deep_review_files": { "type": "integer", "minimum": 1 },
          "max_changed_files": { "type": "integer", "minimum": 1 },
          "max_diff_bytes_per_slice": { "type": "integer", "minimum": 1 }
        }
      }
    },
    "slices": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "paths"],
        "properties": {
          "id": { "type": "string" },
          "paths": {
            "type": "array",
            "items": { "type": "string" }
          },
          "risk": { "enum": ["low", "medium", "high"] }
        }
      }
    },
    "gates": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "argv", "scope"],
        "properties": {
          "id": { "type": "string" },
          "argv": {
            "type": "array",
            "items": { "type": "string" },
            "minItems": 1
          },
          "command": { "type": "string" },
          "scope": {
            "enum": ["staged", "unstaged", "untracked", "merge_base_to_head", "all"]
          },
          "when_paths": {
            "type": "array",
            "items": { "type": "string" }
          },
          "modes": {
            "type": "array",
            "items": { "type": "string" }
          },
          "filter_mode": { "enum": ["added", "diff_context", "file", "nofilter"] },
          "fail_level": { "enum": ["none", "info", "warning", "error"] },
          "blocking": { "type": "boolean" },
          "timeout_seconds": { "type": "integer", "minimum": 1 },
          "final_always": { "type": "boolean" },
          "trusted": { "type": "boolean" },
          "allow_in_ci": { "type": "boolean" },
          "writes_worktree": { "type": "boolean" },
          "requires_network": { "type": "boolean" },
          "trust_reason": { "type": "string" },
          "parallel_safe": { "type": "boolean" },
          "reads_worktree_only": { "type": "boolean" },
          "depends_on": {
            "type": "array",
            "items": { "type": "string" }
          },
          "policy": { "type": "object" },
          "parser": {
            "type": "object",
            "properties": {
              "type": {
                "enum": ["exit-code", "git-diff-check", "regex-lines", "json-diagnostics", "rdjson", "sarif", "checkstyle"]
              }
            }
          }
        }
      }
    }
  }
}
