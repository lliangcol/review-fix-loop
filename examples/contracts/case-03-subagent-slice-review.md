# Case 03: Subagent Slice Review

Each subagent receives one slice or disjoint file set, the current snapshot ID,
and `must_reload` paths. Subagents report findings and evidence; the main agent
owns fixes and final validation.

